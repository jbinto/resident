# rig harness

`golden.py` runs on the corpus machine only (paths are its reality). It is kept here as the
fixtures provenance record — how every byte in `fixtures/` was produced. See fixtures/README.md.
